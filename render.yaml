services:
  - type: web
    name: fin-sight
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: APP_MODE
        value: demo
      - key: FIN_SIGHT_RUNTIME_DIR
        value: /tmp/fin-sight
